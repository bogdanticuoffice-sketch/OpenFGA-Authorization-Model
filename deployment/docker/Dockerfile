# Use a pinned version for reproducible builds.
# The upstream image already sets ENTRYPOINT to the openfga binary.
FROM openfga/openfga:v1.5.3

# Default to run mode. Override with "migrate" for schema migrations.
CMD ["run"]
